# Python
.venv/
__pycache__/
*.pyc

# Node (staticrypt is invoked via npx, no project deps normally)
node_modules/

# OS / editor junk
.DS_Store
Thumbs.db
*.pe4
*.shw
.~lock.*
*.swp

# Corrupted/oversized draft not usable as a source
data/The Bond Trip Book/The Trip Proof 4.pdf

# Superseded per-chapter Word drafts (image-heavy, 25-149MB each - two
# exceed GitHub's 100MB per-file push limit). The_Trip_small.pdf is the
# finished, superseding version and stays tracked.
data/The Bond Trip Book/First draft printed/*.docx
!data/The Bond Trip Book/First draft printed/The Trip Book Master B1.docx

# Unencrypted local preview build - regenerate with `uv run python scripts/build.py`.
# docs/ (the encrypted, GitHub-Pages-served output) IS committed.
/build/
